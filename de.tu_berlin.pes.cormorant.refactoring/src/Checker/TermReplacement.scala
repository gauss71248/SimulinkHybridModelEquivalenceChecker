package Checker
import AbstractRepresentation._
/**
 * @author gauss
 */
//object TermReplacement {
  
//  def replace(f: Term, subf: Term, g:Term): Term = Constant(1.0)
  
//  def atomicReplacement(f: Term, v:Variable, g: Term): Term = f match {
//    case x: Variable if v==x => g
//    case Function(f,xs) if 
//  }
  
//  def containsTerm(f:Term, v: Variable): Boolean = f match {
//    case x: Variable => v==x
//    case Function(f,xs) => xs.foldLeft(false)((a,b)=>a || TermReplacement.containsTerm(b, v))
//    case _ => false
//  }
  
//}

// test